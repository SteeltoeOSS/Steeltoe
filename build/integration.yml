pr:
  branches:
    include:
    - main
    - release/3.*
  paths:
    exclude:
    - /
    include:
    - build/templates
    - build/integration.yml
    - src/Integration

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Integration
