pr:
  paths:
    exclude:
    - build
    - src
    include:
    - .editorconfig
    - stylecop.json
    - '*.props'
    - '*.ruleset'
    - build/templates
    - build/circuitbreaker.yml
    - src/CircuitBreaker

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: CircuitBreaker
