pr:
  branches:
    include:
    - main
    - release/3.*
  paths:
    exclude:
    - /
    include:
    - build/templates
    - build/circuitbreaker.yml
    - src/CircuitBreaker

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: CircuitBreaker
