trigger:
  branches:
    include:
    - main
  paths:
    exclude:
    - '*'
    include:
    - build/templates/*
    - build/discovery.yml
    - src/Discovery/*
pr:
- main

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Discovery
