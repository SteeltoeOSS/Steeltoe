trigger:
  branches:
    include:
    - main
  paths:
    include:
    - build/templates/component-build.yml
    - build/discovery.yml
    - src/Discovery/*
pr:
- main

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Discovery
