trigger:
  branches:
    include:
    - main
  paths:
    include:
    - build/templates/component-build.yml
    - build/messaging.yml
    - src/Messaging/*
pr:
- main

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Messaging
      runRabbitMQ: true
