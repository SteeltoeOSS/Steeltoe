trigger:
  branches:
    include:
    - main
  paths:
    include:
    - build/templates/component-build.yml
    - build/logging.yml
    - src/Logging/*
pr:
- main

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Logging
