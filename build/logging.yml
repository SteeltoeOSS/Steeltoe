trigger:
  branches:
    include:
    - main
  paths:
    exclude:
    - '*'
    include:
    - build/templates/*
    - build/logging.yml
    - src/Logging/*
pr:
- main

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Logging
