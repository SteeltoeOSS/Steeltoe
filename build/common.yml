trigger:
  branches:
    include:
    - main
  paths:
    include:
    - build/templates/component-build.yml
    - build/common.yml
    - src/Common/*
pr:
- main

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Common
