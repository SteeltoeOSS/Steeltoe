trigger:
  branches:
    include:
    - main
  paths:
    exclude:
    - '*'
    include:
    - build/templates/*
    - build/common.yml
    - src/Common/*
pr:
- main

jobs:
  - template: templates/component-build.yaml
    parameters:
      component: Common
